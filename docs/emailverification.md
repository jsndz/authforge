register ->  verified email -> false 
         ->  create a secure token
                            -> store the token
         -> send the user mail with token
         -> user will click on the link
         -> token is send to an api
         -> verified email -> true